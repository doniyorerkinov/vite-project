import Modal from '../../../components/Base/Modal';

function CategoryAddModal({ isCategoryOpen, onCategoryClose }) {
  return (
    <Modal
      isOpen={isCategoryOpen}
      onClose={onCategoryClose}
      title="Добавить сотрудника"
      width="max-w-lg"
    >
      asdasdas
    </Modal>
  );
}
export default CategoryAddModal;
